version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    executor:
      name: node/default
    resource_class: small
    steps:
      - checkout
      - setup_remote_docker
        # - node/with-cache:
        #     steps:
        #       - run: npm install
        #       - run: npm test
        # - run:
        #     name: Run cypress tests
        #     command: ./scripts/run-cypress-tests
      - run: docker-compose build
      - run: docker-compose up --abort-on-container-exit --exit-code-from cypress
workflows:
    build-and-test:
      jobs:
        - build-and-test
